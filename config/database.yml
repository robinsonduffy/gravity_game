# SQLite version 3.x
#   gem install sqlite3
development:
  adapter: postgresql
  database: gravity_game_dev
  username: gravity_game
  password: dbpassword
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  database: gravity_game_test
  host: localhost

production:
  adapter: sqlite3
  database: db/production.sqlite3
  pool: 5
  timeout: 5000
