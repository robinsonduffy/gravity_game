= render "level_factory"
- content_for :custom_js do
  :javascript
    $(document).ready(function(){
      $("#revert-level").remove();
      $("#level-publish-wrapper").remove();
      $("#board").data("level_id","new");
    });